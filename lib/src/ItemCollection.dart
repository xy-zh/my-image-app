class ItemCollection {
  String title, description, postDate, username;
  List<dynamic> imageUrlList;

  ItemCollection(this.title, this.username, this.description,
      this.imageUrlList, this.postDate);
}
